# Jenkins CI/CD Demo - Abdullah Djama

This project demonstrates a simple CI/CD pipeline:
- On commit, Jenkins builds a Docker image.
- Jenkins logs in to DockerHub.
- Jenkins pushes the image to DockerHub.

## Run locally
```bash
docker run abdullahdjama/python-demo:latest
